<!DOCTYPE html>
<html lang="de">
<head>
    <title>${konfigurationsbereich.getNameOderPid()}</title>
    <base href="../../">
    <meta charset="UTF-8">
    <meta name="date" content="2015-12-05"/>
    <meta name="keywords" content="Typen, Mengen, Attributgruppen, Attributlisten, Attribute, Datenkatalog, NERZ, ERZ, VRZ, UZ, Verkehrsrechnerzentrale, Unterzentrale">
    <link rel="stylesheet" type="text/css" href="stylesheet.css" title="Style"/>
</head>
<body>
<div class="header">
    <a href="${konfigurationsbereich.getZustaendiger().getPid()}/${konfigurationsbereich.getPid()}/konfigurationsbereich-ueberblick.html" target="objektFrame">${konfigurationsbereich.getNameOderPid()}</a>
</div>
<div class="content">
    <ul class="objekte">
        #foreach( $objekt in $konfigurationsbereich.getAlleObjekte() )
        <li><a href="${objekt.getBereich().getZustaendiger().getPid()}/${objekt.getBereich().getPid()}/${objekt.getPid()}.html" target="objektFrame">${objekt.getNameOderPid()}</a></li>
        #end
    </ul>
</div>
</body>
</html>
