<!DOCTYPE html>
<html lang="de">
<head>
    <title>Menge ${attributliste.getNameOderPid()}</title>
    <base href="../../">
    <meta charset="UTF-8">
    <meta name="date" content="2015-12-05"/>
    <meta name="keywords" content="Typen, Mengen, Attributgruppen, Attributlisten, Attribute, Datenkatalog, NERZ, ERZ, VRZ, UZ, Verkehrsrechnerzentrale, Unterzentrale">
    <link rel="stylesheet" type="text/css" href="stylesheet.css" title="Style"/>
</head>
<body>
<script type="text/javascript">
    <!--
    try {
        if (location.href.indexOf('is-external=true') == -1) {
            parent.document.title = "${attributliste.getNameOderPid()} (${projekt})";
        }
    }
    catch (err) {
    }
    //-->
</script>
<div class="header">
    <ul>
        <li><a href="ueberblick.html">Überblick</a></li>
        <li><a href="${attributliste.getBereich().getZustaendiger().getPid()}/konfigurationsverantwortlicher-ueberblick.html">Konfigurationsverantwortlicher</a></li>
        <li><a href="${attributliste.getBereich().getZustaendiger().getPid()}/${attributliste.getBereich().getPid()}/konfigurationsbereich-ueberblick.html">Konfigurationsbereich</a></li>
        <li class="active">Objekt</li>
    </ul>
</div>
<div class="subheader">
    <ul>
        <li><a href="index.html?${attributliste.getBereich().getZustaendiger().getPid()}/${attributliste.getBereich().getPid()}/${attributliste.getPid()}.html" target="_top">Mit Frames</a></li>
        <li><a href="${attributliste.getBereich().getZustaendiger().getPid()}/${attributliste.getBereich().getPid()}/${attributliste.getPid()}.html" target="_top">Ohne Frames</a></li>
    </ul>
</div>
<div class="content">
    <p>${attributliste.getBereich().getZustaendiger().getNameOderPid()} / ${attributliste.getBereich().getNameOderPid()}</p>
    <h1>Attributgruppe ${attributliste.getNameOderPid()} (${attributliste.getPid()})</h1>
    <p>${attributliste.getKurzinfo()}</p>
    <p>${attributliste.getBeschreibung()}</p>
    <p>TODO Attribute</p>
</div>
</body>
</html>
