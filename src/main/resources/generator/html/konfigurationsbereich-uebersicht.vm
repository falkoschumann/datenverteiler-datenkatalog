<!DOCTYPE html>
<html lang="de">
<head>
    <title>Konfigurationsbereich ${konfigurationsbereich.getNameOderPid()}</title>
    <base href="../../">
    <meta charset="UTF-8">
    <meta name="date" content="2015-12-05"/>
    <meta name="keywords" content="Typen, Mengen, Attributgruppen, Attributlisten, Attribute, Datenkatalog, NERZ, ERZ, VRZ, UZ, Verkehrsrechnerzentrale, Unterzentrale">
    <link rel="stylesheet" type="text/css" href="stylesheet.css" title="Style"/>
</head>
<body>
<p>${konfigurationsbereich.getZustaendiger().getNameOderPid()}</p>
<h2>Konfigurationsbereich ${konfigurationsbereich.getNameOderPid()} (${konfigurationsbereich.getPid()}))</h2>
<p>${konfigurationsbereich.getKurzinfo()}</p>
<p>${konfigurationsbereich.getBeschreibung()}</p>
#if( !$konfigurationsbereich.getTypen().isEmpty() )
<div class="uebersicht">
    <table border="0" cellpadding="0" cellspacing="0">
        <caption><span>Typen</span></caption>
        <tbody>
        <tr>
            <th>Typ</th>
            <th>Beschreibung</th>
        </tr>
            #set( $row = 1 )
            #foreach( $typ in $konfigurationsbereich.getTypen() )
            <tr>
                <td class="#if ($row % 2 == 0) rowColor #else altColor #end"><a href="${typ.getBereich().getZustaendiger().getPid()}/${typ.getBereich().getPid()}/${typ.getPid()}.html">$typ.getNameOderPid()</a></td>
                <td class="#if ($row % 2 == 0) rowColor #else altColor #end block">${typ.getKurzinfo()}</td>
            </tr>
                #set( $row = $row + 1 )
            #end
        </tbody>
    </table>
</div>
#end
    #if( !$konfigurationsbereich.getMengen().isEmpty() )
    <div class="uebersicht">
        <table border="0" cellpadding="0" cellspacing="0">
            <caption><span>Mengen</span></caption>
            <tbody>
            <tr>
                <th>Menge</th>
                <th>Beschreibung</th>
            </tr>
                #set( $row = 1 )
                #foreach( $menge in $konfigurationsbereich.getMengen() )
                <tr>
                    <td class="#if ($row % 2 == 0) rowColor #else altColor #end"><a href="${menge.getBereich().getZustaendiger().getPid()}/${menge.getBereich().getPid()}/${menge.getPid()}.html">$menge.getNameOderPid()</a></td>
                    <td class="#if ($row % 2 == 0) rowColor #else altColor #end block">${menge.getKurzinfo()}</td>
                </tr>
                    #set( $row = $row + 1 )
                #end
            </tbody>
        </table>
    </div>
    #end
<p>TODO Attributgruppen</p>
<p>TODO Attributlisten</p>
<p>TODO Attribute</p>
</body>
</html>
