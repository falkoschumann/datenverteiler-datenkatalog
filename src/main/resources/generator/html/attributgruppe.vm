<!DOCTYPE html>
<html lang="de">
<head>
    <title>Menge ${attributgruppe.nameOderPid}</title>
    <base href="../../">
    <meta charset="UTF-8">
    <meta name="date" content="2015-12-05"/>
    <meta name="keywords" content="Typen, Mengen, Attributgruppen, Attributlisten, Attribute, Datenkatalog, NERZ, ERZ, VRZ, UZ, Verkehrsrechnerzentrale, Unterzentrale">
    <link rel="stylesheet" type="text/css" href="stylesheet.css" title="Style"/>
</head>
<body>
<script type="text/javascript">
    <!--
    try {
        if (location.href.indexOf('is-external=true') == -1) {
            parent.document.title = "${attributgruppe.nameOderPid} (${projekt})";
        }
    }
    catch (err) {
    }
    //-->
</script>
<div class="header">
    <ul>
        <li><a href="ueberblick.html">Überblick</a></li>
        <li><a href="${attributgruppe.bereich.zustaendiger.pid}/konfigurationsverantwortlicher-ueberblick.html">Konfigurationsverantwortlicher</a></li>
        <li><a href="${attributgruppe.bereich.zustaendiger.pid}/${attributgruppe.bereich.pid}/konfigurationsbereich-ueberblick.html">Konfigurationsbereich</a></li>
        <li class="active">Objekt</li>
    </ul>
</div>
<div class="subheader">
    <ul>
        <li><a href="index.html?${attributgruppe.bereich.zustaendiger.pid}/${attributgruppe.bereich.pid}/${attributgruppe.pid}.html" target="_top">Mit Frames</a></li>
        <li><a href="${attributgruppe.bereich.zustaendiger.pid}/${attributgruppe.bereich.pid}/${attributgruppe.pid}.html" target="_top">Ohne Frames</a></li>
    </ul>
</div>
<div class="content">
    <p>${attributgruppe.bereich.zustaendiger.nameOderPid} / ${attributgruppe.bereich.nameOderPid}</p>
    <h1>Attributgruppe ${attributgruppe.nameOderPid} (${attributgruppe.pid})</h1>
    <p>${attributgruppe.kurzinfo}</p>
    #if( !$attributgruppe.beschreibung.empty )
    <p>${attributgruppe.beschreibung}</p>
    #end
    #if( !$attributgruppe.attribute.empty )
        <div class="ueberblick">
            <table border="0" cellpadding="0" cellspacing="0">
                <caption><span>Attribute</span></caption>
                <tbody>
                <tr>
                    <th>Name</th>
                    <th>Attributtyp</th>
                    <th>Anzahl</th>
                    <th>Anzahl ist</th>
                    <th>Beschreibung</th>
                </tr>
                    #set( $row = 1 )
                    #foreach( $attribut in $attributgruppe.attribute )
                    <tr>
                        <td class="#if ($row % 2 == 0) rowColor #else altColor #end">${attribut.name}</td>
                        <td class="#if ($row % 2 == 0) rowColor #else altColor #end"><a href="${attribut.attributTyp.bereich.zustaendiger.pid}/${attribut.attributTypbereich.pid}/${attribut.attributTyp.pid}.html">${attribut.attributTyp.name}</a></td>
                        <td class="#if ($row % 2 == 0) rowColor #else altColor #end">${attribut.anzahl}</td>
                        <td class="#if ($row % 2 == 0) rowColor #else altColor #end">
                            #if( ${attribut.anzahlVariabel} )
                                variabel
                            #else
                                fest
                            #end
                        </td>
                        <td class="#if ($row % 2 == 0) rowColor #else altColor #end block">
                            #if( $attribut.kurzinfo && !$attribut.kurzinfo.empty )
                                ${attribut.kurzinfo}
                            #else
                                <span class="inherited">${attribut.attributTyp.kurzinfo}</span>
                            #end
                        </td>
                    </tr>
                        #set( $row = $row + 1 )
                    #end
                </tbody>
            </table>
        </div>
    #end
    <p>TODO Für Attribute ergänzen: Typ</p>
</div>
</body>
</html>
