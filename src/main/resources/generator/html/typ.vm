<!DOCTYPE html>
<html lang="de">
<head>
    <title>Typ ${typ.nameOderPid}</title>
    <base href="../../">
    <meta charset="UTF-8">
    <meta name="date" content="2015-12-05"/>
    <meta name="keywords" content="Typen, Mengen, Attributgruppen, Attributlisten, Attribute, Datenkatalog, NERZ, ERZ, VRZ, UZ, Verkehrsrechnerzentrale, Unterzentrale">
    <link rel="stylesheet" type="text/css" href="stylesheet.css" title="Style"/>
</head>
<body>
<script type="text/javascript">
    <!--
    try {
        if (location.href.indexOf('is-external=true') == -1) {
            parent.document.title = "${typ.nameOderPid} (${projekt})";
        }
    }
    catch (err) {
    }
    //-->
</script>
<div class="header">
    <ul>
        <li><a href="ueberblick.html">Überblick</a></li>
        <li><a href="${typ.bereich.zustaendiger.pid}/konfigurationsverantwortlicher-ueberblick.html">Konfigurationsverantwortlicher</a></li>
        <li><a href="${typ.bereich.zustaendiger.pid}/${typ.bereich.pid}/konfigurationsbereich-ueberblick.html">Konfigurationsbereich</a></li>
        <li class="active">Objekt</li>
    </ul>
</div>
<div class="subheader">
    <ul>
        <li><a href="index.html?${typ.bereich.zustaendiger.pid}/${typ.bereich.pid}/${typ.pid}.html" target="_top">Mit Frames</a></li>
        <li><a href="${typ.bereich.zustaendiger.pid}/${typ.bereich.pid}/${typ.pid}.html" target="_top">Ohne Frames</a></li>
    </ul>
</div>
<div class="content">
    <p>${typ.bereich.zustaendiger.nameOderPid} / ${typ.bereich.nameOderPid}</p>
    <h1>Typ ${typ.nameOderPid} (${typ.pid})</h1>
    <p><strong>Alle direkten Supertypen</strong></p>
    #if( !$typ.superTypen.empty )
    <p>
    #foreach( $superTyp in $typ.superTypen )
    <a href="${superTyp.bereich.zustaendiger.pid}/${superTyp.bereich.pid}/${superTyp.pid}.html" target="objektFrame">${superTyp.nameOderPid}</a>#if( $velocityHasNext ), #end
    #end
    </p>
    #end
    #if( !$typ.subTypen.empty )
    <p><strong>Bekannte direkte Subtypen</strong></p>
    <p>
    #foreach( $subTyp in $typ.subTypen )
        <a href="${subTyp.bereich.zustaendiger.pid}/${subTyp.bereich.pid}/${subTyp.pid}.html" target="objektFrame">${subTyp.nameOderPid}</a>#if( $velocityHasNext ), #end
    #end
    #end
    </p>
    <p>${typ.kurzinfo}</p>
    <p>${typ.beschreibung}</p>
    #if( !$typ.mengen.empty )
    <div class="ueberblick">
        <table border="0" cellpadding="0" cellspacing="0">
            <caption><span>Mengen</span></caption>
            <tbody>
            <tr>
                <th>Menge</th>
                <th>Beschreibung</th>
            </tr>
                #set( $row = 1 )
                #foreach( $mengenVerwendung in $typ.mengen )
                <tr>
                    <td class="#if ($row % 2 == 0) rowColor #else altColor #end"><a href="${mengenVerwendung.mengenTyp.bereich.zustaendiger.pid}/${mengenVerwendung.mengenTyp.bereich.pid}/${mengenVerwendung.mengenTyp.pid}.html">${mengenVerwendung.mengenName}</a></td>
                    <td class="#if ($row % 2 == 0) rowColor #else altColor #end block">${mengenVerwendung.mengenTyp.kurzinfo}</td>
                </tr>
                    #set( $row = $row + 1 )
                #end
            </tbody>
        </table>
    </div>
    #end
    #if( !$typ.attributgruppen.empty )
        <div class="ueberblick">
            <table border="0" cellpadding="0" cellspacing="0">
                <caption><span>Attributgruppen</span></caption>
                <tbody>
                <tr>
                    <th>Attributgruppe</th>
                    <th>Beschreibung</th>
                </tr>
                    #set( $row = 1 )
                    #foreach( $attributgruppe in $typ.attributgruppen )
                    <tr>
                        <td class="#if ($row % 2 == 0) rowColor #else altColor #end"><a href="${attributgruppe.bereich.zustaendiger.pid}/${attributgruppe.bereich.pid}/${attributgruppe.pid}.html">${attributgruppe.name}</a></td>
                        <td class="#if ($row % 2 == 0) rowColor #else altColor #end block">${attributgruppe.kurzinfo}</td>
                    </tr>
                        #set( $row = $row + 1 )
                    #end
                </tbody>
            </table>
        </div>
    #end
</div>
</body>
</html>
